{
  "name": "chain-map-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "ingest": "tsx src/ingest-runner.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fast-xml-parser": "^4.5.0",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "tsx": "^4.7.3",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
